export const metadata = {
  title: 'Logic Korean',
  description: 'Learn Korean grammar with logic.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
